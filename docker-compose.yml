version: "3.8"
services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./uploads:/app/uploads       # persiste uploads en host
      - ./:/app                      # monta código para desarrollo
    environment:
      - FLASK_SECRET_KEY=insecure-secret-key
    restart: unless-stopped